{
  "name": "clean-web",
  "version": "1.0.0",
  "description": "goal is to block problematic images from rendering in html pages (mostly as chrome extension but the infrastructure is generic)",
  "main": "src/index.js",
  "scripts": {
    "test": "npm run test:unit",
    "test:unit": "jest",
    "test:e2e": "echo 'E2E tests require Playwright browsers to be installed. Run: npm run install:playwright' && playwright test",
    "test:unit:watch": "jest --watch",
    "test:e2e:headed": "playwright test --headed",
    "install:playwright": "playwright install",
    "test:integration": "jest tests/unit/integration.test.js"
  },
  "keywords": [
    "chrome-extension",
    "image-filter",
    "content-blocker"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.1",
    "jest": "^30.1.3",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2"
  }
}
